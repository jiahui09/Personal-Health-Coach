# Supabase Free 后端（可选）。
# 两项都不填 -> 使用本地 localStorage 演示数据（MockHealthRepository）。
# 两项都填   -> 使用 SupabaseHealthRepository（浏览器直连 Supabase REST，行级安全 RLS）。
# 注意：anon key 是公开键，安全边界在数据库的 RLS 策略，而不是这个 key。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
